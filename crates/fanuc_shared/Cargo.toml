[package]
name = "fanuc_shared"
version = "0.1.0"
edition = "2021"

[features]
server = ["dep:bevy"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
bevy = { workspace = true, optional = true }

